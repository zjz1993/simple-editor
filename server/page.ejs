<!DOCTYPE html>
<html>
<head>
    <title><%= htmlWebpackPlugin.options.title %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width; initial-scale=1">
</head>
<body>
<div id="root"></div>
</body>
<script>
    var SimpleEditor = window.simpleEditor;
    new SimpleEditor(document.getElementById('root')).init({
        onChange: (html) => {
            console.log('变化啊');
            console.log(html);
        },
        placeHolder:'请输入',
        actions: ['bold', 'underline', 'italic'],
    })
    // const SimpleEditor=window.simpleEditor;
    // const editor=new SimpleEditor(document.getElementById('root'));
    // editor.create();
</script>
</html>
